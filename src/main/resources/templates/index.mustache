{{>layout/header}}


<h1> 스프링 부트로 구현하는 김영기 웹 서비스 </h1>
<div class="col-md-12">
    <div class="row">
        <div class="col-md-6">
            <a href="/posts/save" role="button" class="btn btn-primary">글 등록</a>
            {{#userName}} <!-- 머스테치는 if문이 없다 true / false로만 구분-->
                Logged in as: <span id="user">{{userName}}</span>
                <a href="/logout" class="btn btn-info active" role="button">Logout</a> <!--스프링시큐리티에서 제공하는 로그아웃 URL -->
            {{/userName}}
            {{^userName}}  <!-- 해당 값이 없는경우일때 -->
                <a href="/oauth2/authorization/google" class="btn btn-success active" role="button">Google Login</a>
                <a href="/oauth2/authorization/naver" class="btn btn-secondary active" role="button">Naver Login</a>
            {{/userName}}
        </div>
    </div>
    <br>
    <!-- 목록 출력 영역 -->
    <table class="table table-horizontal table-bordered">
        <thead class="thead-strong">
        <tr>
            <th>글번호</th>
            <th>제목</th>
            <th>작성자</th>
            <th>최종수정일</th>
        </tr>
        </thead>
        <tbody id="tbody">
        {{#posts}}  <!-- 머스테치 문법 리스트 돌려서 찍어주는거(for) -->
            <tr>
                <td>{{id}}</td>   <!-- 리스트에서 뽑은 객체 핃드값 -->
                <td><a href="/posts/update/{{id}}">{{title}}</a></td>
                <td>{{author}}</td>
                <td>{{modifiedDate}}</td>
            </tr>
        {{/posts}}
        </tbody>
    </table>
</div>
<!--
<div class="col-md-12">
    <div class="row">
        <div class="col-md-6">
            <a href="/secretPage" role="button" class="btn btn-primary">
                테스트용
            </a>
        </div>
    </div>
</div>
-->

{{>layout/footer}}
